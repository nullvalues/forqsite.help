---
id: INFRA-001
rail: INFRA
title: README.md: revisit generated-artifact/do-not-hand-edit framing for the refresh workflow
status: draft
phase: "2"
auth_gated: false
schema_introduces: false
primary_files:
  - README.md
touches:
---

## Requires

- CONTENT-001 through CONTENT-004 ideally complete or scheduled, since this story's
  wording should describe the workflow those stories actually went through (built via
  the pairmode loop, not hand-edited outside it).

## Ensures

- README.md's "Updating" section is updated to reflect: these files were originally
  generated by a Claude design session from the forqsite repo; nothing has kept them in
  sync since; periodic refresh passes (like Phase 2) happen through the normal pairmode
  build loop — spec written as stories, built and reviewed per-story — not as
  freestanding hand-edits outside that process.
- A follow-up skill to auto-sync these docs on each forqsite commit is noted as planned
  but not yet built — do not overstate its existence.
- The rest of README.md's structure and content (Why this repo exists, Contents,
  Hosting, Scope) is left untouched; this story only touches the Updating section's
  wording.
- No em dashes introduced in the new wording (this project deliberately reads plainly —
  see CONTENT-004's prose-decluttering rationale for why).

## Instructions

Keep the edit short and factual. Don't describe the auto-sync skill as if it already
exists, and don't claim this phase's stories are complete if they aren't yet — write in
terms of the workflow going forward, not a specific past event.

## Tests

- `grep -c "do not hand-edit"` (or equivalent) on README.md reflects the updated framing.
- Diff confirms only the Updating section changed.
