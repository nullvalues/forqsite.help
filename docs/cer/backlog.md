# forqsite.help — Cold-Eyes Review (CER) Backlog

*Last updated: 2026-09-21*

This file is the structured triage log for findings from external cold-eyes reviews.
Each finding is assigned to one quadrant. Findings are not deleted — resolved findings
remain in place with a resolution marker.

A **resolution marker** closes a finding by placing one of three keywords at the start of an annotation segment. The keywords are `RESOLVED`, `SUPERSEDED`, and `OBSOLETE` (case-insensitive: `resolved`, `Resolved`, `RESOLVED` all match). An annotation segment begins at the start of text, after a `|` table cell boundary, after sentence-ending punctuation followed by whitespace, or at an emphasis or bracket opener (`**`, `(`, `[`). Accepted forms include `**RESOLVED Phase 10 — INFRA-1**` (bolded), `| Resolved cp-10 — INFRA-1 |` (after a cell boundary), `**SUPERSEDED by CER-001**`, and `**OBSOLETE — disproved**`. A keyword appearing mid-clause — preceded by a space and a word — is not a marker and leaves the finding open; `this should be resolved before cp-10` and `UNRESOLVED naming gap` do not close. The `cer-do-now` checkpoint guard (a harness rule, not defined in this repo) scans this marker form to enforce that all Do Now findings are either marked closed or re-triaged before each checkpoint.

---

## Do Now

Urgent and important. Blocks correctness, security, or the next phase.

| ID | Finding | Source | Date | Phase |
|----|---------|--------|------|-------|


| — | *(none)* | — | — | — |
| CER-025 | `printf '%q'` emits bash-specific `$'...'` quoting for some values, and the remote command runs under the far account's login shell, which may be POSIX `sh`. Portability rather than exposure, but the failure mode is that the quoting protecting the deploy silently stops protecting it. | security-auditor (CP-11) | 2026-09-22 | 11 |
| CER-026 | `scripts/drift-check.sh` prints sidecar fields extracted with `sed` straight to the terminal. The pattern excludes quotes but permits control and ANSI sequences, so a hostile origin can garble or forge the report block an operator is about to read and paste. Fix shape: sanitise each field to its expected character class at extraction. | security-auditor (CP-11) | 2026-09-22 | 11 |
| CER-027 | Each deploy writes backup copies in the remote directory and prunes none, so they accumulate without bound. Not served — the bind-mounts are per-file — so this is growth only. | security-auditor (CP-11) | 2026-09-22 | 11 |


---

## Do Later

Important, not urgent. Quality improvements, architectural refinements.

| ID | Finding | Source | Date | Phase |
|----|---------|--------|------|-------|



| CER-001 | CONTENT-004 missed one named acceptance-criterion line item: reword one of GAP-006/GAP-011's near-identical 'exactly the case ... exists to catch/covers' construction in gap-handoff.html. Both phrases still present verbatim. Suggested fix for GAP-011: '...requires the same restart the admin banner already flags.' **SUPERSEDED cp-10 — re-checked against the current gap-handoff.html on 2026-09-21 at 5318bb1, not against this row's own description: GAP-011 was pruned from the bundle in Phase 5 (3f16dcc), leaving one occurrence of the construction, in GAP-006. The pair the finding names no longer exists, so there is nothing to reword. The row outlived its target because it was filed against a bundle state that changed three phases before anyone re-read it (CONTENT-027).** | intent-reviewer (Phase 2 checkpoint) | 2026-07-16 | 2 |
| CER-002 | GAP-012 (and the Known gaps convention generally) cites forqsite source paths and a commit hash on a publicly served page. Matches the page's existing citation convention, so not a Phase 8 regression — needs an operator ruling on whether product-repo internal detail belongs in public docs. **RESOLVED cp-10 — operator ruled KEEP: the cited paths name the reader's own source tree, and the citations are what make the site's claims checkable. Recorded as deliberate publication policy in docs/ideology.md § Accepted constraints (CONTENT-026)** | security-auditor (Phase 8 checkpoint) | 2026-09-18 | 8 |
| CER-003 | docs/architecture.md's Deployment section names the internal host and its deployment paths in a public repo. Pre-existing since the Phase 4 containerization commit, outside Phase 9's diff. Same public-hygiene class as CER-002. **RESOLVED cp-10 — generalised in docs/architecture.md, docs/phases/phase-4.md and docs/stories/INFRA/INFRA-002.md; the identifiers were redacted from this row too, under the same principle** | security-auditor (Phase 9 checkpoint) | 2026-09-21 | 9 |
| CER-004 | Operator ruling wanted: whether docs/pairmode-wiring-audit.md's references to a version-pinned flex cache path under a home directory and to flex-internal story IDs are acceptable in a public repo, or should be generalized. Judged LOW-severity disclosure, not blocking. **RESOLVED cp-10 — the audit doc was removed from this repository in CONTENT-025 (its subject is the build harness, not forqsite); the cache path was redacted from this row too, under the same principle applied to CER-003** | security-auditor (Phase 9 checkpoint) | 2026-09-21 | 9 |
| CER-005 | index.html's `sc-for` ledger list does not hydrate under headless Chromium at a `file://` origin, so ledger rows can be verified in source but not in a render. Pre-existing, reproduced on the pre-story bundle. Limits reviewer verification coverage for any story touching that list. **Corrected in Phase 10.** Re-run under control on 2026-09-21 at 5318bb1 (CONTENT-027), 2x2 across both origins and both URL fragments: the stated cause is wrong and the substance is broader. With the fragment held constant the `file://` and `http://127.0.0.1` dumps are byte-identical after blob-URL normalisation, so the behaviour is origin-independent and the zero-dependency guarantee is unaffected. After stripping `<script>` and `<style>` blocks, zero `GAP-0NN` ids appear in rendered markup at either origin — all nine matches live in the inert `const ledger = [...]` source. The `#gaps` fragment renders no screen at all at either origin (no `data-screen-label` node); the no-fragment load renders Overview at both. Ledger rows are therefore verifiable in source but not in a render, not because of the origin but because that screen does not render. The earlier contrary observation was a whole-dump grep counting script source as rendered nodes (CER-012). This finding stays open; Phase 10 records the corrected evidence and does not repair the gap. | reviewer (CONTENT-019, accepted deviation) | 2026-09-18 | 8 |
| CER-006 | Flex effort recording is unreliable in this harness: `read_completed_spawn` parses a worker's verdict from its final message text, but some subagents deliver the WORKER-004 block via the hand-back channel and sign off in prose, leaving `outcome` NULL. Original finding (phases 8-9): six rows needed hand reconciliation, each carrying a `notes` field saying so. Report upstream to flex rather than patching the cache. **Grew in Phase 10.** Measured against the effort database as of 2026-09-21: of 36 attempt rows, 13 carry a hand-reconciliation note (9 from the phase 8-9 work — rows 15, 16, 17, 18, 21, 24, 25, 31, 32 — and 4 added by Phase 10's own stories: CONTENT-023 rows 33/34, CONTENT-024 rows 35/36), and 6 still carry a NULL `outcome` (ids 1, 3, 5, 6, 9, 11, all builder attempts from July 2026 — a different and older population, predating the reconciliation practice). The original six-row count could not be reproduced from the database and is left as the historical claim it was rather than silently restated. CONTENT-025 wrote the symptom and the transcribed rows up for the project that owns the defect, operator-local and untracked. Referring it upstream does not fix it here, so this finding stays open. | orchestrator (phases 8-9, updated Phase 10) | 2026-09-21 | 9 |
| CER-007 | Phases 3-7 were marked complete in docs/phases/index.md without ever passing a green build gate (the prose `test_command` failed red at exit 127 throughout). Open operator question whether to back-tag them. **RESOLVED cp-10 — operator ruled: leave untagged; no `cp-3`..`cp-7` tag is created. The ruling and its reason are recorded in docs/checkpoints.md (CONTENT-026) and are not restated here.** | CONTENT-020 audit | 2026-09-21 | 9 |
| CER-009 | A frozen-snapshot probe asserted against the effort database cannot pass once its own story is built: the build's and the review's own attempt rows land inside the same calendar day the assertion is stamped for, so a date-only boundary is perturbed by the very run it is meant to verify. Observed on CONTENT-025, whose probe measured 38 rows against an asserted 36. Suggested convention: bound such probes on an attempt-id captured before the build starts, not on a same-day date filter. | reviewer (CONTENT-025) | 2026-09-21 | 10 |
| CER-010 | The date-and-commit stamp convention is scoped to claims about forqsite's behaviour, so a point-in-time claim about harness telemetry carries a date only. CER-006 demonstrates the gap: a date-granularity stamp cannot flag a figure that moves several times within the stamped day. Proposal to extend the convention to harness-telemetry claims of this kind; recorded as a proposal, not a defect. | reviewer (CONTENT-025) | 2026-09-21 | 10 |
| CER-011 | An Ensure that greps for a verbatim multi-word phrase inside a prose document fails when the phrase soft-wraps across two source lines, although the content is correct. Fired live during CONTENT-026's build. The check conflates the invariant it means to test (the reason is recorded in exactly one place) with an accident of Markdown wrapping, and a future cosmetic reflow would break it with a failure that reads as a content regression. Fix shape: normalise whitespace before matching rather than a line-oriented grep. Applies to any future story using this pattern, not only this file. | reviewer (CONTENT-026) | 2026-09-21 | 10 |
| CER-012 | A render assertion made by grepping a headless `--dump-dom` output counts matches inside inert `<script>` template source as though they were rendered nodes. This produced a false "all nine ledger rows rendered" observation, recorded in a phase doc as a disproof and nearly used to close a true finding as obsolete; stripping `<script>` blocks first shows zero rendered rows at either origin. A comparison meant to isolate one variable must also hold the others constant — the same reproduction varied the URL fragment and the origin together, so its diff measured the fragment. Fix shape: strip script blocks before asserting, or assert on a structural marker only the renderer emits. | orchestrator (CONTENT-027 re-run) | 2026-09-21 | 10 |
| CER-013 | Phase 10's orchestrator instructed builders to commit with bare `git add`/`git commit`, overriding the builder procedure's explicit "do not commit" and bypassing `commit-story-worktree`, which stages exactly the declared `touches` scope and proves the commit landed. Cause: a builder reporting complete with every declared file unstaged was read as builder error, when it is the documented CER-274/CER-331/CER-332 failure mode the command exists to fix. Four stories landed this way; each was verified by hand to contain only its declared scope, so no bad commit resulted, but the guard was replaced by orchestrator attention. Fix shape: land stories with `commit-story-worktree`. | orchestrator (Phase 10, flagged by the CONTENT-027 builder) | 2026-09-21 | 10 |
| CER-014 | cp-8 and cp-9 were both checkpointed green over a live site two phases stale: the site served Phase 7 content from 2026-09-16 while the repository stood at Phase 9, including a factual correction that had never reached readers. No gate compared served bytes to committed bytes — every check compared the repository to itself, which is a proxy for the question that matters. Found by hand on 2026-09-21 while deploying. Fourth instance of the pattern CER-009, CER-011 and CER-012 describe. **RESOLVED cp-11 — Phase 11's drift check (INFRA-007) landed; the checkpoint-sequence manual step CONTENT-028 added to docs/checkpoints.md is the other half.** | orchestrator (Phase 11 planning) | 2026-09-22 | 11 |
| CER-015 | `scripts/deploy.sh`'s unrecognized-argument branch exits 2, the same code the exit-code table assigns to "configuration missing", so a caller cannot distinguish a bad flag from an unset host or directory variable by exit code alone. Not a contract violation — the table assigns no code to bad usage — but a loose reuse in a script whose exit codes are its interface. Fix shape: give bad usage its own code and add it to the table. | reviewer (INFRA-006) | 2026-09-22 | 11 |
| CER-016 | The landing step is split across two procedures that disagree. The builder procedure says builders do not commit; the reviewer procedure carries an "On PASS, commit" block that issues bare `git add`/`git commit`; and `CLAUDE.build.md`'s loop names neither, going builder to reviewer to `merge-story-worktree` (CER-013). Consequence observed on INFRA-007: an orchestrator instruction to leave the story uncommitted so it could be landed with `commit-story-worktree` was overridden by the reviewer's own procedure, which committed on PASS. The commit happened to contain exactly the declared scope, so no harm resulted, but which component lands a story is currently decided by whichever instruction the worker reads last. Same root as CER-013 and referred with it; not fixed here, as the build harness is not this project's subject (CONTENT-025). | orchestrator (INFRA-007 review) | 2026-09-22 | 11 |
| CER-017 | An `awk` range probe anchored on a heading pattern at both ends — `awk '/^### Title/,/^### /'` — closes on its own start line and prints 0 for a block of any length, so a length assertion built on it can never fail. Present in CONTENT-028's Tests and true of the sibling entries it was modelled on. Measuring a last-in-section entry by scanning to the next heading also overshoots, giving three different numbers for one block (14, 22, 34) depending on method. Fix shape: bound the range on the block's own fields, or count the field lines directly, and state the method next to the number. | orchestrator (CONTENT-028 build) | 2026-09-22 | 11 |
| CER-018 | A backlog row that describes its own planned closure in prose is read by `cer.is_resolution_marked` as already closed. CER-014 was filed with the sentence "Resolved by Phase 11's drift check...", and because that word follows sentence-ending punctuation it sits exactly where the marker grammar looks, so an open finding reported itself as closed to the checkpoint guard for the life of the phase. The grammar is behaving as CONTENT-023 specced it; the row was written badly. Fix shape: when filing, state the intended closure in the future tense or name the story without the keyword, and probe a newly filed row before trusting it. | orchestrator (CONTENT-028 spec) | 2026-09-22 | 11 |
| CER-019 | `scripts/deploy.sh` passes the configured alias to `ssh` as its first argument without validating it. A value beginning with `-` is consumed as an option rather than a destination, and `ssh` has no `--` terminator, so `-oProxyCommand=...` is local command execution as the invoking user before any connection. Bounded by the value being operator-supplied, but an environment variable does not otherwise grant execution. Fix shape: constrain the alias to `[A-Za-z0-9._-]+` where the configuration is read. | security-auditor (CP-11) | 2026-09-22 | 11 |
| CER-020 | `scripts/drift-check.sh`'s failure paths print curl's stderr and the redirect target verbatim, both of which embed the configured base URL, and `docs/checkpoints.md` directs the operator to record that output in a committed public file. INFRA-007's hygiene assertion covers only the success block, so the one an operator pastes on a failing run is untested. Fix shape: substitute a placeholder token for the configured URL before printing, and extend the assertion to the failure blocks. | security-auditor (CP-11) | 2026-09-22 | 11 |
| CER-021 | Neither fetch in `scripts/drift-check.sh` sets `--max-filesize` or `--proto`. A hostile origin can stream for the full 60-second budget into the scratch directory, and `file:///etc` was verified accepted, turning the check into a local-file reader that would report ok against a planted file. Fix shape: bound the size and restrict the scheme. | security-auditor (CP-11) | 2026-09-22 | 11 |
| CER-022 | The 2026-09-22 ruling that the public site URL is an instance identifier lives only in story docs. `docs/ideology.md` § Name the class, not the instance still reads "a host name, an absolute path or a machine name", so a future gate enforces a narrower constraint than the one given, which is why the URL survives in `README.md:6`, `docs/architecture.md:75`, `docs/phases/phase-4.md`, and `docs/stories/INFRA/INFRA-002.md`. Fix shape: record the extension in the rule itself, then scrub those files. | security-auditor (CP-11) | 2026-09-22 | 11 |
| CER-023 | `scripts/deploy.sh` stages each file at a predictable remote name (bundle name plus a one-second stamp) and writes with `cat >`, which follows an existing symlink. An account other than the deploy user with write access to that directory could pre-plant a symlink and redirect the bytes. The required exclusive ownership of the remote directory is currently undocumented. Fix shape: stage via a remote `mktemp`, or state the precondition. | security-auditor (CP-11) | 2026-09-22 | 11 |
| CER-024 | `scripts/deploy.sh` and `scripts/drift-check.sh` `source` the config file after `cd`-ing to whatever repository the caller's cwd resolves to, so running either from inside an untrusted clone that ships a `scripts/deploy.env` executes its contents. Fix shape: parse `KEY=value` lines instead of sourcing, or refuse when the repo root is not the script's own. | security-auditor (CP-11) | 2026-09-22 | 11 |
| CER-028 | `scripts/deploy.sh` runs its six `ssh` invocations with inherited stderr, so `ssh`'s own failure text ("could not resolve hostname") and the remote shell's ("cd: ... No such file or directory") reach the terminal naming the configured alias and directory. Its own `echo` lines are clean and the success block is asserted, so this is the transport's output rather than the script's. Kept separate from CER-020 because the fix shape differs: capturing `ssh`'s stderr changes its interactive behaviour for password and host-key prompts, which is a design question. No procedure directs deploy output into a committed file, so the forcing function differs too. | spec-writer (INFRA-009 recon) | 2026-09-22 | 11 |
| CER-029 | One published claim carries an older verification stamp than every other. `gap-handoff.html`'s "What isn't ready yet" page states the ordering assumption "production stays non-docker: network Postgres and MinIO, with compose as a dev convenience" and stamps it 2026-09-16 against `nullvalues/forqsite@17b78645`; the three other stamps across both bundles cite `7089b9dc`. So the sequencing advice on that page rests on a premise nobody has re-checked against the current product, and it is load-bearing — if production moved to docker, the ordering it justifies is wrong. Noticed by the operator on the live site, 2026-09-21. Fix shape: re-verify the claim against the current product commit and re-stamp, or record why the older commit is still the right anchor. Not a deploy defect; the live site matches the repository. | operator (live site) | 2026-09-22 | 11 |



---

## Do Much Later

Not urgent, marginal value. Style, cosmetics, speculative improvements.

| ID | Finding | Source | Date | Phase |
|----|---------|--------|------|-------|


| — | *(none)* | — | — | — |


---

## Do Never

Rejected findings. Record the rejection reason so it is not re-raised.

| ID | Finding | Source | Date | Phase | Resolution |
|----|---------|--------|------|-------|------------|


| CER-008 | The internal host name and deployment paths generalised out of the working tree in Phase 10 remain in this repository's git history, in every commit from the Phase 4 containerization onward; a `git log -S` search still surfaces them. No edit to the working tree reaches history. | orchestrator (Phase 10, CONTENT-024) | 2026-09-21 | 10 | Rejected: the only remedy is a history rewrite and force-push, which invalidates the `cp-N` checkpoint tags this project uses as its verification record. If these identifiers ever become genuinely sensitive, the correct response is to rotate them, not to rewrite history. Recorded so the question is not re-raised at each security gate. |

