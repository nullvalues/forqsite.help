# CLAUDE.build.md — forqsite.help Build Orchestrator

You are the build orchestrator for the forqsite.help project. Drive the build loop by
delegating to `~/flex-marketplace-cache/flex-0.4.7/skills/pairmode/scripts/flex_build.py next-action` and the appropriate leaf worker. Do not write code,
review code, or commit directly — those are leaf-worker responsibilities.

pairmode_scripts_dir = ~/flex-marketplace-cache/flex-0.4.7/skills/pairmode/scripts

## Build loop

Story-build actions (`spawn-builder`, `spawn-reviewer`, and the reviewer-equivalent spawn actions that write and commit code) run inside a disposable per-story git worktree: created fresh from the current branch tip before the builder spawns, and on reviewer PASS rebased + fast-forward-merged back onto the main branch, or on reviewer FAIL discarded outright — untracked content and all — without ever touching the main worktree's files. The builder and reviewer operate inside the returned worktree path, never the main project directory. Checkpoint-stage workers (`checkpoint-security`, `checkpoint-intent`, `checkpoint-docs`) are read-mostly/advisory and never commit — they stay on the main worktree, unwrapped. Planning-doc writes are not worktree-scoped: `phase_new.py` appends a `| <phase> | <title> | planned |` row to the active era doc's phase ledger (its `Phases` section) on every scaffold, so any commit of scaffolded planning docs stages `docs/eras/` alongside `docs/phases/` and `docs/stories/` — an unstaged ledger row is silent working-tree drift (CER-082). **One iteration per story (CER-074):** the loop polls `next-action` once per story — the builder spawn, the reviewer spawn, and the merge or discard all happen inside that single iteration; `next-action` never emits `spawn-reviewer`, and the orchestrator dispatches the reviewer itself after the builder returns. Do not re-poll `next-action` between the builder's return and the merge: a story counts as passed only once its `story-<ID>` commit reaches the main branch, which only the merge creates — so a mid-story poll reads the finished attempt as failed and re-dispatches a wasteful second builder over a good build. **Never commit `docs/phases/permissions/<ID>.json` (CER-326):** it is the per-story permissions manifest `create-story-worktree` generates and both `merge-story-worktree` and `discard-story-worktree` clear automatically (INFRA-238) — it is excluded from the `dirty-checkout` merge guard's `git status --porcelain` check (`_main_checkout_dirty_paths`, INFRA-487) precisely so it never needs a commit to keep the checkout clean. A `chore(<ID>): ...` commit of this file is orchestrator improvisation over an untracked-looking file, not a required step, and `next_story.py`'s `_has_story_commit` now excludes all `chore(` commits from build evidence for exactly this reason — this line is belt-and-braces alongside that reader-side fix, not a substitute for it.

`leaf-worker-for(a.action)` spawns `Task`/`Agent` with `subagent_type` resolved from `a.action` via the fixed map below (never `general-purpose` for a story-build/checkpoint action — an unresolved `general-purpose` spawn is invisible to the context-budget gate, INFRA-199/INFRA-241); `model=a.model` always overrides an agent's frontmatter `model:` default (see `docs/architecture.md` § Spawn contract):

ACTION_SUBAGENT_TYPE = {spawn-builder: builder, spawn-reviewer: reviewer, spawn-loop-breaker: loop-breaker, spawn-security-auditor: security-auditor, spawn-intent-reviewer: intent-reviewer, checkpoint-security: security-auditor, checkpoint-intent: intent-reviewer, checkpoint-docs: docs-reviewer, spawn-spec-writer: spec-writer}  # INFRA-325 adds checkpoint-docs: docs-reviewer — the role was fully specced (WORKER-011) but had no scaffolded shell or dispatch entry until this story; other spawn/checkpoint actions keep their own existing dispatch, out of INFRA-241 scope. INFRA-344 adds spawn-spec-writer: spec-writer — this map entry is the subagent_type source for the explicit spawn-spec-writer branch in the loop below (spawn, then commit the elaborated spec); the full CLAUDE.build.md/.j2 reconciliation (other divergences between the two files) is INFRA-342's job, sequenced after this story. INFRA-422 (Phase 148): the `spawn-gate-worker: gate-worker` entry and its explicit dispatch branch below are removed — the judged-gate dispatch capability was never exercised across this project's build history, so `next_action.py`'s Row 4b now resolves a tripped schema/auth judged gate the same fail-closed way the stub gate's Row 4a already does, without emitting `spawn-gate-worker`.

```
while true:
    a = ~/flex-marketplace-cache/flex-0.4.7/skills/pairmode/scripts/flex_build.py next-action --json --project-dir .
    if a.meta.warnings is non-empty: report each entry to the operator verbatim — INFRA-538's `shadow_review=`concurrent`` unsupported-in-0.4.x entry is the one populated case today, and it names the exact Build-standards/frontmatter edit that turns the setting off; offer that edit, then proceed with `a.action` either way — `meta.warnings[]` is advisory only and never blocks (DP2), so this report-and-continue step never gates the loop.
    if a.action == "done": break
    if a.action == "spawn-builder":
        wt = ~/flex-marketplace-cache/flex-0.4.7/skills/pairmode/scripts/flex_build.py create-story-worktree --story-id a.scalar --project-dir . --model-selection-reason a.reason; build_result = spawn leaf-worker-for("spawn-builder") with subagent_type=ACTION_SUBAGENT_TYPE["spawn-builder"], scalar=a.scalar, model=a.model, cwd=wt  # also stamps current_story (+ INFRA-348's model_selection_reason, from this same a.reason) + generates docs/phases/permissions/<scalar>.json (INFRA-238)
        spawn leaf-worker-for("spawn-reviewer") with subagent_type=ACTION_SUBAGENT_TYPE["spawn-reviewer"], scalar=a.scalar, model=<first line of `~/flex-marketplace-cache/flex-0.4.7/skills/pairmode/scripts/flex_build.py select-reviewer-model --story-id a.scalar --attempt a.meta.attempt --project-dir .`>, cwd=wt, builder_reason=build_result.reason  # INFRA-318: reviewer_model: floor's live seam — next-action never emits spawn-reviewer (CER-074); INFRA-456: builder_reason is the builder's returned BUILD-RESULT reason text, included because reviewer bounded input 10 has no other source for builder dispositions
        if a.meta.shadow is present: the resolver has already decided coverage and pre-selected the model — dispatch `Task`/`Agent` with subagent_type=shadow-reviewer, model=a.meta.shadow.model, scalar=a.meta.shadow.scalar, cwd=wt (the builder's own worktree) at builder-spawn time; nothing here re-derives this decision from the `shadow_review` flag. Arm the wake source (INFRA-525) right after that first dispatch, in the background: `~/flex-marketplace-cache/flex-0.4.7/skills/pairmode/scripts/next_action.py shadow-watch --worktree-dir wt`. On its exit (0 = the growth threshold was crossed, 3 = `--max-wait` elapsed with no growth — both are normal outcomes, neither is an error), run `~/flex-marketplace-cache/flex-0.4.7/skills/pairmode/scripts/next_action.py shadow-gate --worktree-dir wt` (optionally with `--question '<text>'` when you hold a specific question worth asking, INFRA-525 Instruction 6) and dispatch one more shadow-reviewer (model=a.meta.shadow.model, same as the initial dispatch, scalar=a.meta.shadow.scalar, cwd=wt) iff its printed `dispatch` field is `true`; then re-arm `shadow-watch` and repeat. Never dispatch while a previous shadow-reviewer is still in flight — that serialization is what makes a lock file unnecessary; `shadow-watch` itself never dispatches, writes to the worktree, or advances the bookmark, it only turns growth into a process exit you receive as a check-in. The reading is now deterministic — `shadow-gate` takes it from the seeded/advancing bookmark, never from anything held in your own working memory — but arming the watcher and issuing the dispatch remain orchestrator actions this tooling does not enforce: `next-action` is never polled mid-build, so nothing but your own wake ticks makes this happen, and a build that never arms `shadow-watch` after the first dispatch simply never gets a second one. Mechanism described in `skills/pairmode/skills/shadow-reviewer/procedure.md` (repo-relative; fall back to `~/flex-marketplace-cache/flex-0.4.7/skills/pairmode/scripts/../../../skills/pairmode/skills/shadow-reviewer/procedure.md` in a project that has not vendored `skills/`, INFRA-527/INFRA-528) § Self-directed single pass and § Mid-build re-dispatch is now a scheduled check-in (INFRA-455, INFRA-475, INFRA-524, INFRA-525).  # INFRA-475: the *existence* of the initial dispatch moves to the resolver (next-action's spawn-builder action carries meta.shadow whenever shadow_review resolves to "concurrent" — Build standards default, overridable per-story via the story's own shadow_review: frontmatter; since INFRA-524 only the attempt-2 retry emission carries it — the seeded baseline bookmark replaced the empty builder-spawn first pass, so an attempt-1 story dispatches no shadow-reviewer at spawn time); this replaces the former "if shadow_review == concurrent" prose gate and the separate `model_selector.py --role shadow-reviewer` CLI call it used to make for the initial dispatch. INFRA-455's activity-gated mid-build re-dispatch cadence replaces INFRA-452's interval re-dispatch with activity-gated dispatch; each dispatch is a single self-directed pass reading its own prior state from a bookmark line it appends to .pairmode-suggestions.md, no lock file, no marker. INFRA-525 makes the *reading* of that gate deterministic (`shadow-gate` parses the bookmark and the worktree's own diff instead of the reading living in orchestrator working memory) and adds `shadow-watch` as the wake source that makes the gate evaluable at all — arming the watcher, dispatching on its exit, and re-arming still stay orchestrator judgment above, since `next-action` itself is never polled mid-build.
        wait for builder AND (shadow-reviewer, if dispatched) to both complete before the merge/discard step below — a shadow-reviewer still running when the worktree is torn down would have its shared filesystem removed out from under it mid-read (activity-gated dispatch means a shadow-reviewer may still be in flight when the builder returns — the orchestrator never dispatches a second one while one is running, but the one already running is not aborted; if it is still running when the builder finishes, wait for it rather than proceeding)
        on reviewer PASS: ~/flex-marketplace-cache/flex-0.4.7/skills/pairmode/scripts/flex_build.py merge-story-worktree --story-id a.scalar --project-dir .  # also clears the attempt counter (INFRA-237), the current_story/permissions stamps (INFRA-238), and the recorded gate verdict (INFRA-341)
        on reviewer FAIL: ~/flex-marketplace-cache/flex-0.4.7/skills/pairmode/scripts/flex_build.py discard-story-worktree --story-id a.scalar --project-dir .  # also clears the current_story/permissions stamps (INFRA-238) and the recorded gate verdict (INFRA-341)
    elif a.action == "spawn-spec-writer": spawn leaf-worker-for("spawn-spec-writer") with subagent_type=ACTION_SUBAGENT_TYPE["spawn-spec-writer"], scalar=a.scalar, model=a.model; regardless of the returned SPEC-RESULT status ("done" or "revised"), run, in the main project directory (never inside a worktree — spec writes are not worktree-scoped): `git add docs/stories/<RAIL>/a.scalar.md && git commit -q -m "spec(a.scalar): elaborate stub story"` before the loop's next next-action poll  # INFRA-344/F10: without this explicit commit step, create-story-worktree branches a worktree off a HEAD that predates the spec-writer's elaboration. The commit is scoped to exactly the one story spec path, never `git add -A` or a directory.
    else:
        spawn leaf-worker-for(a.action) with subagent_type=ACTION_SUBAGENT_TYPE[a.action], scalar=a.scalar, model=a.model  # INFRA-328: for spawn-loop-breaker, a.reason carries the double-fail story's most recent recorded fail_cause (or "" if none found); build the LOOP-BREAKER: [error] | FILE: [file:line] | TRIED: [what failed] prompt (CLAUDE.md § Loop-breaker mode) from it before spawning, extracting a FILE: value from a.reason when it names one
    # effort-attempt recording AND the attempt counter are fully hook-side (INFRA-236, INFRA-237): post_tool_use.py's Task/Agent branch calls subagent_transcript.record_attempt_from_transcript() after every spawn — writes an effort.db row and bumps .companion/attempt_counter.json on a FAIL outcome — no separate orchestrator-side recording step needed.
```

## Model-upgrade prompts

At any judgment-handoff pause whose reason involves a model choice (`model-upgrade`
or future model-selection handoffs): present the suggested model(s) as named
`AskUserQuestion` options, and **always** leave a free-text path (the "Other"
input) so the operator can key in any model name — the `model_selector.py` tiers are not guaranteed current or exhaustive.

## Checkpoint

Execute each checkpoint leaf worker as dispatched. After each returns, call:
  ~/flex-marketplace-cache/flex-0.4.7/skills/pairmode/scripts/flex_build.py record-checkpoint-step <action> --project-dir . --phase-key <phase-key> --verdict <verdict>
where `<verdict>` is that worker's own REVIEW-RESULT verdict (PASS/FAIL/ALIGNED) — `--verdict` is required for every gate step (INFRA-543). `checkpoint-security` and `checkpoint-docs` are **blocking**: a recorded verdict other than PASS (or a missing verdict) makes `checkpoint-tag` refuse later in this sequence, exit 5. `checkpoint-intent` is recorded but never blocks (INTENT-REVIEWER's "never blocks a checkpoint outright") — its verdict is still legible via `checkpoint-report`'s gate-verdicts block below. After the three gate workers complete and before `checkpoint-report`, run `PATH=$HOME/.local/bin:$PATH uv run python ~/flex-marketplace-cache/flex-0.4.7/skills/pairmode/scripts/dark_feature_scan.py --project-dir .` (INFRA-458) and print its output verbatim, then record it (its exit code is advisory only — never a hard block, INTENT-REVIEWER's "never blocks a checkpoint outright") with `~/flex-marketplace-cache/flex-0.4.7/skills/pairmode/scripts/flex_build.py record-checkpoint-step dark-feature-scan --project-dir . --phase-key <phase-key> --verdict PASS` on scan exit 0, or `--verdict FAIL` otherwise — its blocking posture is unchanged (still advisory-only), only its legibility. Then call `~/flex-marketplace-cache/flex-0.4.7/skills/pairmode/scripts/flex_build.py checkpoint-report --project-dir .` and print its output verbatim (cost rollup + next-phase pointer + the four gate steps' recorded verdicts) before checkpoint-tag. Then re-run next-action. checkpoint-tag (mandated order, CER-083): 1) `record-checkpoint-step checkpoint-tag --project-dir . --phase-key <phase-key>` (no `--verdict` — checkpoint-tag records none of its own and rejects one if supplied; resets checkpoint_step; marks the phase complete in `docs/phases/index.md` **and** flips its row in the active `docs/eras/` doc's phase ledger, INFRA-267), then commit both paths — `git add docs/phases/index.md docs/eras/` — before tagging; 2) `git tag cp-<phase-key> && git push origin main --tags`. If step 1 exits 5 (a blocking gate's recorded verdict is not PASS, or was never recorded), that is a refusal to be fixed and retried, never worked around: fix the finding, re-run that gate worker, and re-record its verdict — never by deleting the recorded verdict or hand-editing `state.json`. A raw `git tag` alone, skipping step 1, is forbidden: `record-checkpoint-step` is idempotent and safely re-runnable if step 2 fails after it, but if the order reverses, step 1's skip is silent and the next phase's gates are lost. `--phase-key <phase-key>` on every call is what stops the wrong phase being marked complete (CER-077): it is the explicit source of truth `record-checkpoint-step`'s precedence chain checks first, ahead of any re-derivation from `docs/phases/index.md`.

**Build standards** (per-project facts the builder/reviewer procedure skills read from here instead of hardcoding, INFRA-240): test_command=`none — static HTML, open file:// or serve with any static file server` | test_dir=`tests/` | protected_paths=`(none)` | domain_isolation_rule=`(none)` | intent_review=`(unset)` | shadow_review=`concurrent` | covered_contracts=`(none)` — when set to `pre-build` (INFRA-315), a `spawn-intent-reviewer` whose scalar is a phase key (not a story ID) is the pre-build case: spawn `intent-reviewer` with `model=null`, then record its verdict via `~/flex-marketplace-cache/flex-0.4.7/skills/pairmode/scripts/flex_build.py record-intent-review --phase-key <a.scalar> --verdict <PASS|FAIL|ALIGNED> --project-dir .` before re-running `next-action` — this fires once per phase; a non-PASS/ALIGNED verdict pauses for the operator instead of proceeding. `covered_contracts` (INFRA-317) is a comma-separated list of `doc-section::source-file` pairs — each pair names a doc-section↔source-file "covered contract" (a structured payload shape no database enforces) the builder must read both halves of before touching either file; the `::` separator was chosen because it cannot appear in a doc-section heading or a repo-relative path, unlike `/` or `:` alone; unset renders `(none)`, matching `protected_paths`'s pattern. `shadow_review` (INFRA-359, INFRA-475): the resolved mode — this project-level value, overridden in either direction by a story's own `shadow_review:` frontmatter when present — is now decided by `next-action` itself, not by orchestrator prose reading this flag. When the resolved mode is `concurrent`, only the attempt-2 retry `spawn-builder` action (Row 5) carries a `meta.shadow` block (`role`, `scalar`, `model`, `model_reason` — the last two pre-selected via `model_selector.select_shadow_reviewer_model`); the `spawn-builder` branch above executes that co-spawn rather than deciding whether one is warranted. INFRA-524 (Phase 166, CER-299) removed the attempt-1 and next-story-after-PASS co-spawns: the builder-spawn dispatch always read a fresh, empty worktree and provably found nothing, and `create-story-worktree` now seeds the shadow baseline bookmark (`head=<worktree HEAD> worktree=0f/0+/0-`) into `.pairmode-suggestions.md` at creation instead — a deliberate coverage trade: an attempt-1 story that passes review receives no shadow dispatch at all in this interim window, since the activity-gated mid-build re-dispatch below is itself keyed off `a.meta.shadow` (choosing when the first real dispatch fires is INFRA-525's story, pinned immediately after in this phase's build order), and `merge-story-worktree`'s INFRA-476 gate is narrowed to match (`concurrent` AND recorded attempt count > 0). Absent, `(unset)`, or any other value leaves `meta.shadow` absent from every emitted action, byte-identical to pre-INFRA-359 behavior (the same absent-means-opted-out pattern as `intent_review`). The mid-build re-dispatch cadence (INFRA-455's activity gate, in the `spawn-builder` branch above) is unaffected and stays orchestrator judgment — only the *existence* of the initial dispatch is now resolver-decided. INFRA-538 (2026-08-21, operator ruling 2026-08-19): setting `shadow_review` to the value `concurrent` is unsupported in 0.4.x and deferred to 0.5.x — the role's designed purpose (findings on disk before the story commit) is not delivered, because arming `shadow-watch` and issuing the dispatch remain orchestrator actions this tooling does not enforce. Ships dark rather than removed: bootstrap's accept-on-empty default no longer opts a fresh project in, `create-story-worktree` only seeds the shadow bookmark when the resolved mode is `concurrent`, and (whenever a project or story still opts in) every `next-action` emission carries the unsupported-mode entry in `meta.warnings[]` — see the loop's `a.meta.warnings` handling above.

## All other input

Read `CLAUDE.md` and apply the reviewer role.
